version: cycle_discovery_v1
description: Optional manual anchors for auditing. Cycle labels are generated by deterministic
  rules, not by manual labels.
manual_seed_events: []
matching:
  tolerance_hours: 12
commonality:
  fallback_to_cycle_config: true
